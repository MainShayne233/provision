#!/usr/bin/env bash

install() {
  (
    cd "$OPT_DIR"
    wget https://github.com/erroneousboat/slack-term/releases/download/v0.4.1/slack-term-linux-amd64
    mv slack-term-linux-amd64 slack-term
    chmod +x slack-term
  )
}

verify() {
  which_verify "slack-term"
}
