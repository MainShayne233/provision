#!/usr/bin/env bash

install() {
  provision install "bazel"
  sudo apt install autoconf coreutils parallel
  (
    cd /tmp
    git clone https://github.com/sorbet/sorbet.git
    cd sorbet
    bazel build //main:sorbet --config=dbg
  )
}

verify() {
  which_verify "sorbet"
}
